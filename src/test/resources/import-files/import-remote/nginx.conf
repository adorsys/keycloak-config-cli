
http {

  server {

    location /import {
        root /data/www/import;
        auth_basic off;
    }

    location /import/secured {
      auth_basic           "Restricted";
      auth_basic_user_file /etc/nginx/.htpasswd;
    }
  }
}
